A standalone information radiator for a Last.fm users now-playing track.

h2. The Good

* Entirely client side, no server logic, only JSONP
* Has two modes, single load and auto refresh (with a configurable interval)

h2. The Bad

* Dependant on jQuery
* Uses example Last.fm API key (needs fixing)